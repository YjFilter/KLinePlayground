# GitHub Private Upload (2026-07-21)

- Created private repository: `YjFilter/KLinePlayground`.
- Kept the existing Gitee remote as `origin`; added GitHub as remote `github`.
- Upload scope includes the current crypto replay, drawing, order, performance, Vercel, Neon, tests, and project handoff changes.
- Excluded `.runtime/`, `.env.local`, `.vercel/`, `users/`, offline market data, and external credential files.
- Secret scans found no GitHub tokens, private keys, or embedded production database credentials in the upload scope or Git history.
- The latest full verification before upload was 675 tests and 66 subtests passing, plus JavaScript syntax, Python compile, and diff checks.
